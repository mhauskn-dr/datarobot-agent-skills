# AIG-004 — No evaluation / eval harness for prompts

Determine whether the repository contains any automated evaluation or
regression testing of prompt/agent behavior (eval datasets, LLM-as-judge,
trajectory assertions, golden outputs). Absence across the whole repo is a
single file-level finding (`line: null`).

Output: follow `prompts/_contract.md`.
